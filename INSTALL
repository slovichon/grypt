$Id$

Install Notes For Grypt
=======================

Apply the file named `patch' outside the main Gaim source root:

	$ patch < gaim-2.0.0beta3/plugins/grypt/patch

Then proceed to run Gaim usual installation process:

	./configure
	make && make install
