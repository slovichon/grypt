Install Notes For Grypt

Apply the file named `patch' outside the main Gaim directory:

	$ ls -d gaim-0.66
	gaim-0.66
	$ patch < gaim-0.66/plugins/grypt/patch

Now run Gaim's ./configure && make install. Grypt should
compile and install correctly.

And that's it!
